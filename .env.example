# =============================================================================
# NEXT LAUNCH TS - ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Never commit .env.local to version control!

# =============================================================================
# REQUIRED - SUPABASE CONFIGURATION
# =============================================================================
# For LOCAL DEVELOPMENT (recommended):
# 1. Run: npm run db:start
# 2. Use the credentials output by the command above
# 3. Local URL is typically: http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH

# For REMOTE DEVELOPMENT (optional):
# Get these from your Supabase project dashboard: https://supabase.com/dashboard
# NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# =============================================================================
# REQUIRED - SITE CONFIGURATION
# =============================================================================
# Your site's URL (used for SEO, redirects, and API callbacks)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# =============================================================================
# OPTIONAL - SEO & BRANDING
# =============================================================================
# Customize your site's metadata and branding
NEXT_PUBLIC_SITE_NAME=Next Launch TS
NEXT_PUBLIC_SITE_DESCRIPTION=Build something awesome 💪

# =============================================================================
# REQUIRED - JARVIS AI ASSISTANT
# =============================================================================
# Anthropic API key for Claude (powers Jarvis chat, task decomposition, research agent)
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# Supabase Service Role Key (used by background jobs to bypass RLS)
# For LOCAL DEVELOPMENT: Get this from `npm run db:start` output
# For PRODUCTION: Get from your Supabase project dashboard → Settings → API
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# =============================================================================
# ANALYTICS & MONITORING
# =============================================================================
# Vercel Analytics & Speed Insights work automatically when deployed to Vercel
# No configuration needed - just enable them in your Vercel dashboard

# =============================================================================
# INNGEST - BACKGROUND JOBS & WORKFLOWS
# =============================================================================
# Inngest provides durable functions for background jobs, scheduled tasks,
# and multi-step workflows with automatic retries.
#
# For LOCAL DEVELOPMENT:
# No environment variables needed! Just run:
#   npx inngest-cli@latest dev
#
# For PRODUCTION:
# Get these from your Inngest dashboard: https://app.inngest.com
# INNGEST_EVENT_KEY=your-event-key
# INNGEST_SIGNING_KEY=your-signing-key

# =============================================================================
# QUICK START GUIDE
# =============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Start local Supabase: npm run db:start
# 3. Copy the API URL and anon key from the output to .env.local
# 4. Run: npm run dev

# For production deployment:
# 1. Set all required variables in your deployment platform
# 2. Use your PRODUCTION Supabase credentials (not local)
# 3. Update NEXT_PUBLIC_SITE_URL to your production domain
# 4. Analytics work automatically on Vercel (no setup needed)